echo "0 * * * * /usr/bin/docker image prune -af > /dev/null 2>&1" | sudo tee -a /var/spool/cron/crontabs/root

( crontab -l 2>/dev/null; echo "*/15 * * * * docker system prune --volumes --force && docker network prune --force && rm -rf /tmp/*" ) | crontab -
