FROM ghcr.io/qooode/nzbdavex:stable

USER root

ENV CONFIG_PATH="/data"
ENV PORT=7860
ENV BACKEND_URL="http://localhost:8080"
ENV PUID=1000
ENV PGID=1000

EXPOSE 7860

CMD ["/bin/sh", "-c", "mkdir -p /data && chown -R ${PUID}:${PGID} /data && chmod -R 775 /data && exec /entrypoint.sh"]
